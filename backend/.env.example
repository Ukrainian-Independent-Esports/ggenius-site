# GGenius Backend Environment Configuration
# Конфігурація середовища для бекенду GGenius

# ===========================================
# JWT Configuration / Налаштування JWT
# ===========================================

# SECRET_KEY — обов'язковий, мінімум 32 символи
# Generate a secure key using: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-super-secret-key-minimum-32-characters-long

# JWT Algorithm (default: HS256)
JWT_ALGORITHM=HS256

# Access Token expiration in minutes (default: 15)
ACCESS_TOKEN_EXPIRE_MINUTES=15

# Refresh Token expiration in days (default: 30)
REFRESH_TOKEN_EXPIRE_DAYS=30

# ===========================================
# Telegram Bot Configuration
# ===========================================

# Bot Token from @BotFather
BOT_TOKEN=your-telegram-bot-token

# ===========================================
# CORS Configuration / Налаштування CORS
# ===========================================

# Allowed origins (comma-separated)
# For development: http://localhost:3000,http://localhost:5173
# For production: https://ggenius.gg
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,https://ggenius.gg

# ===========================================
# Database (if needed)
# ===========================================

# DATABASE_URL=postgresql://user:password@localhost:5432/ggenius

# ===========================================
# API Configuration
# ===========================================

# API_HOST=0.0.0.0
# API_PORT=8000
# DEBUG=false
